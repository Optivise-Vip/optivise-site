Fix the TypeScript or lint errors in this project. 

Run `npx tsc --noEmit` mentally against the current files. For each error:
1. Show the file, line, and error message
2. Explain in one sentence why it's wrong
3. Show the fix

Do not change logic — only fix type errors and lint violations. Do not add `any` as a fix.

$ARGUMENTS
