Create a new React component for this project. Requirements:

- TypeScript strict — typed props interface, no `any`
- Tailwind only for styling — no inline styles
- If it contains text or copy, import it from `app/lib/site.ts`
- If it has scroll or animation state, use refs not useState
- PascalCase filename matching the component name
- Place it in `app/components/` unless told otherwise
- Export as named export, not default

The component to create: $ARGUMENTS
